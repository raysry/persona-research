## 任务

请实现 `solution.py` 中的 `max_sliding_window` 函数。

给定一个整数列表 `nums` 和一个滑动窗口大小 `k`，返回每个窗口中的最大值。

窗口从最左侧滑动到最右侧，每次移动一位。

## 要求

- 时间复杂度优于 O(n*k)（建议使用单调队列）

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #239 Sliding Window Maximum (Hard)
