## 任务

请实现 `solution.py` 中的 `reverse_string` 函数。

给定一个字符列表 `s`，原地反转该列表。不要分配额外的数组，必须通过修改输入列表来实现。

## 要求

- 原地修改输入列表，不返回任何值
- 不能使用额外的数组空间（O(1) 额外空间）

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #344 Reverse String (Easy)
