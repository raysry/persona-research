## 任务

请实现 `solution.py` 中的 `is_palindrome` 函数。

给定一个整数 `x`，判断它是否是回文数。回文数是指正序和倒序读都一样的整数。

## 要求

- 负数不是回文数
- 不要将整数转换为字符串来判断

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #9 Palindrome Number (Easy)
