## 任务

请实现 `solution.py` 中的 `two_sum` 函数。

给定一个整数列表 `nums` 和一个目标值 `target`，找出列表中和为目标值的两个数的下标。

## 要求

- 每个输入只有一个有效答案
- 不能使用同一个元素两次
- 返回的下标顺序不限

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #1 Two Sum (Easy)
