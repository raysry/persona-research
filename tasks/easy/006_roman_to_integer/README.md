## 任务

请实现 `solution.py` 中的 `roman_to_int` 函数。

给定一个罗马数字字符串，将其转换为整数。

罗马数字规则：I=1, V=5, X=10, L=50, C=100, D=500, M=1000。
特殊规则：IV=4, IX=9, XL=40, XC=90, CD=400, CM=900（小值在大值左边表示减法）。

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #13 Roman to Integer (Easy)
