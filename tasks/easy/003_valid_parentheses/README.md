## 任务

请实现 `solution.py` 中的 `is_valid` 函数。

给定一个只包含 `'('`、`')'`、`'{'`、`'}'`、`'['`、`']'` 的字符串，判断字符串是否有效。

有效字符串需满足：左括号必须用相同类型的右括号闭合；左括号必须以正确的顺序闭合；每个右括号都有一个对应的左括号。

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #20 Valid Parentheses (Easy)
