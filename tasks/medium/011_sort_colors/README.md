## 任务

请实现 `solution.py` 中的 `sort_colors` 函数。

给定一个包含红色（0）、白色（1）和蓝色（2）的列表 `nums`，原地排序使得相同颜色相邻，按照 0、1、2 顺序排列。

## 要求

- 原地修改，不返回值
- 不能使用内置排序函数
- 要求一趟扫描完成（O(n) 时间，O(1) 空间）

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #75 Sort Colors (Medium)
