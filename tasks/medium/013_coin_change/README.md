## 任务

请实现 `solution.py` 中的 `coin_change` 函数。

给定不同面额的硬币列表 `coins` 和一个总金额 `amount`，计算凑成总金额所需的最少硬币个数。如果无法凑成，返回 -1。

## 要求

- 每种硬币数量无限
- 无法凑成时返回 -1

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #322 Coin Change (Medium)
