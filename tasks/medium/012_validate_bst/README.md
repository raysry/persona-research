## 任务

请实现 `solution.py` 中的 `is_valid_bst` 函数。

给定一棵二叉树（用层序列表表示，None 表示空节点），判断它是否是一棵有效的二叉搜索树（BST）。

有效 BST 定义：
- 节点的左子树只包含小于当前节点的值
- 节点的右子树只包含大于当前节点的值
- 左右子树本身也必须是 BST

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #98 Validate Binary Search Tree (Medium)
