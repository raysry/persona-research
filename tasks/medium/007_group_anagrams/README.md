## 任务

请实现 `solution.py` 中的 `group_anagrams` 函数。

给定一个字符串列表 `strs`，将其中的字母异位词分组。字母异位词是指字母相同但排列不同的字符串。

## 要求

- 返回分组后的列表（每组内部顺序不限，组间顺序不限）
- 空字符串也是有效输入

## 约束

- 不要修改 `test_solution.py`
- 只修改 `solution.py`

来源：LeetCode #49 Group Anagrams (Medium)
